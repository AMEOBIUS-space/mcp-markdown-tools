# MCP Markdown Tools — Markdown Operations for AI Agents

> MCP server for markdown operations. 16 tools, zero deps, pure Python stdlib.

## Tools
to_html, generate_table, generate_list, generate_link, generate_image, generate_header, generate_code_block, generate_blockquote, generate_hr, validate, extract_links, extract_images, extract_headers, extract_code_blocks, strip_markdown, word_count

## Tests
```bash
python -m pytest tests/ -v  # 44 tests, all passing
```

## License
MIT — AMEOBIUS
